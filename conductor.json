{
  "$schema": "https://conductor.build/schemas/conductor.json",
  "scripts": {
    "setup": "bun install --frozen-lockfile --ignore-scripts --minimum-release-age=604800",
    "run": "scripts/dev.sh",
    "archive": "scripts/archive-workspace.sh"
  },
  "runScriptMode": "concurrent"
}
